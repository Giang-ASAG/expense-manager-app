class OnboardingEntity {
  final String title;
  final String description;
  final String imagePath; // Đường dẫn đến ảnh trong thư mục assets

  OnboardingEntity({
    required this.title,
    required this.description,
    required this.imagePath,
  });
}
